services:
  app:
    build: .
    container_name: mission-impossible-app
    ports:
      - "5000:5000"
    environment:
      SECRET_KEY: change-me-in-production
      DATABASE_URL: sqlite:////app/backend/instance/game.db
      PORT: 5000
    volumes:
      - ./backend/instance:/app/backend/instance